**HTTPS** (HyperText Transfer Protocol Secure) — это расширение HTTP, которое шифрует данные между клиентом и сервером. Для этого используется TLS (Transport Layer Security), который защищает данные от перехвата и подмены.